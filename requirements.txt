# Model + preprocessing
torch>=2.3
transformers>=4.52.0        # Qwen2.5-Omni support
accelerate>=0.34
qwen-omni-utils[decord]     # converts image/audio/video inputs for Qwen2.5-Omni models
pillow
soundfile
librosa

# Local vector database
chromadb>=0.5

# Terminal UI
textual>=0.80

numpy
